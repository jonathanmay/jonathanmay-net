# Status — portfolio

> Snapshot of the current state. Rewrite this each session (it is not a log).

## Where things stand

Live and deployed; polished static site with theme toggle and scroll-reveal animations. Two pages: `index.html` (CV + projects) and `manual.html` ("A User Manual to Me", added 2026-07-20). The manual is linked from the site nav, from the Contact section on `index.html`, and back from the manual's own hero — so it is reachable on mobile, where the nav links are hidden below 620px. Clean git history, no work in flight.

## In progress

- (nothing in flight)

## Known issues

- `docs/` is copied into the deployed image and served publicly (`.dockerignore`'s `*.md` only matches the repo root). Backlog item 1; not yet fixed.

## Blockers

- None.
